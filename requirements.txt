# ARIA — backend dependencies
# Core web
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.2

# Data layer
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
greenlet>=3.0

# Async tasks / cache / streams
celery>=5.3
celery-redbeat>=2.2
redis>=5.0

# LLM (provider-agnostic: Groq primary, Ollama fallback)
groq>=0.11
ollama>=0.3

# Agent orchestration
langgraph>=0.2

# HTTP / templating
httpx>=0.27
jinja2>=3.1

# Observability
structlog>=24.1

# Security / crypto
cryptography>=42.0
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# Data / demo
faker>=25.0

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
ruff>=0.5
